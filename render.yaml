databases:
  - name: edusphere-db
    plan: free
    databaseName: plateforme_scolaire
    user: edusphere

services:
  - name: edusphere-backend
    type: web
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: edusphere-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 8h
      - key: EPHEMERE_LIEN_BASE_URL
        sync: false
        # Valeur définie manuellement dans le dashboard Render (non versionnée
        # ici pour ne pas être écrasée) : https://edusphere-frontend-pryg.onrender.com/acces-temporaire
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM
        sync: false
        # Render bloque le SMTP sortant sur son plan gratuit — Resend (API
        # HTTP) est la voie qui fonctionne ici. Les variables SMTP_* restent
        # supportées par le code pour un dev local ou un hébergeur qui
        # autorise le SMTP, mais RESEND_API_KEY est prioritaire si présente.
        # Sans aucune des deux, les e-mails sont simulés (console.log). Voir
        # backend/.env.example pour le détail des deux options.

  - name: edusphere-frontend
    type: web
    runtime: docker
    plan: free
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    envVars:
      - key: BACKEND_SCHEME
        value: https
      - key: BACKEND_HOST
        # Les deux services se joignent en HTTPS public (Render ne route pas
        # de trafic privé vers un service gratuit). Render a suffixé le nom du
        # service backend (edusphere-backend-fh30 au lieu de edusphere-backend) —
        # cette valeur DOIT rester synchronisée avec l'URL réelle du service
        # edusphere-backend (visible en haut de sa page Render), sous peine
        # d'être écrasée par une resynchronisation du Blueprint.
        value: edusphere-backend-fh30.onrender.com
